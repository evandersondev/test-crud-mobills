import React from "react";
import CircularProgress from "@material-ui/core/CircularProgress";

export default function CircularUnderLoad() {
  return <CircularProgress thickness={5} size={50} disableShrink />;
}
